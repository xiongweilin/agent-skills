interface:
  display_name: "Verification Before Completion"
  short_description: "Require fresh evidence before completion claims"
  default_prompt: "Use $verification-before-completion to run fresh checks and verify the evidence before declaring a task complete."
