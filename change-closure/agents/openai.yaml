interface:
  display_name: "Change Closure"
  short_description: "Run explicitly authorized closure checks"
  default_prompt: "Use $change-closure only because completion verification was explicitly requested or a specific mandatory gate requires closure; perform only the minimum checks required by that scope."
