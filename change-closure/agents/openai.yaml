interface:
  display_name: "Change Closure"
  short_description: "Close durable changes with fresh verification"
  default_prompt: "Use $change-closure after this authorized durable change to run fresh checks, sync affected contracts, sweep stale identifiers, and list leftovers."
