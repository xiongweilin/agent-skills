interface:
  display_name: "Chezmoi Management"
  short_description: "Edit source, apply, check sync once"
  default_prompt: "Use $chezmoi-management for a chezmoi-managed file: edit the authoritative source, apply it, perform one canonical synchronization check, then commit or push only when the managed-file workflow requires it."
