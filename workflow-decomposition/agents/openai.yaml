interface:
  display_name: "Workflow Decomposition"
  short_description: "Map durable cross-system workflows"
  default_prompt: "Use $workflow-decomposition to break this workflow into states, guards, human nodes, side effects, and metrics."
