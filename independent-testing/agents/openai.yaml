interface:
  display_name: "Independent Testing"
  short_description: "Run the smallest sufficient independent test pass"
  default_prompt: "Use $independent-testing because this task explicitly identifies you as the independent-testing subagent; establish only the stated material obligations with the smallest sufficient evidence, then return PASS, FAIL, or REOPEN."
