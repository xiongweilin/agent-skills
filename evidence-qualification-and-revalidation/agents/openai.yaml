interface:
  display_name: "Evidence Qualification and Revalidation"
  short_description: "Reuse evidence unless a material assumption changed"
  default_prompt: "Use $evidence-qualification-and-revalidation only when a concrete material change may have invalidated a specific assumption of prior evidence; otherwise reuse the prior qualification."
